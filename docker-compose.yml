version: '3.8'

services:
  Heart_Disease_Prediction_app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    restart: unless-stopped

    environment:
      - PYTHONUNBUFFERED=1
